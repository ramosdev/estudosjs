const myMap = new Map();

myMap.set ('Apple', 'Fruit'); // seta primeira a chave e depois o valor

myMap.get('Apple');

myMap.delete ("Apple");

// maps podem ter chaves de qualquer tipo

//maps possuem propriedade length

// maps são fáceis de iterar

// utilizando quando não conhecemos o valor da chave

// os valores tem o mesmo tipo

const mySet = new Set();

mySet.add (1); // quando quer adicionar um valor

mySet.has (1); // quando quer verificar se existe um valores

mySet.delete(1); //deletar o valor existente 